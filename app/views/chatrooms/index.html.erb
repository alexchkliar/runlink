<% @chatrooms.each do |chatroom| %>
  <% if chatroom.user_id == current_user.id%>
    <%= link_to User.find(chatroom.recipient_id).first_name, chatroom_path(chatroom) %>
  <% else %>
    <%= link_to User.find(chatroom.user_id).first_name, chatroom_path(chatroom) %>
  <% end %>
  <br>
<% end %>
