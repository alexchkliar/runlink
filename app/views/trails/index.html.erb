<h1>Select a trail!</h1>
<% @trails.each do |trail|%>
  <ul>
    <li><%= link_to trail.name, trail_path(trail) %></li>
    <li><%= trail.location %></li>
    <li><%= trail.distance %>km</li>
    <li><%= trail.difficulty %></li>
    <li><%= trail.rating %> ⋆⋆⋆⋆⋆</li>
    <li>Posted by: <%#= trail.user.name %>...</li>
  </ul>
<% end %>
